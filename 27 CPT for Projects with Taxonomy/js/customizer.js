/* ( function( $ ) {
    wp.customize( 'ajax_theme_three_hero_image', function( value ) {
        value.bind( function( newVal ) {
            $( '.hero-image img' ).attr( 'src', newVal );
        } );
    } );
} )( jQuery ); */