When the user clicks the button:

React JSON Injection using PHP (Hard Coded)

A POST request is sent to admin-ajax.php

The message is handled in PHP

A response is returned and shown via alert() or console.log()

